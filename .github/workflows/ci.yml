name: Build and Test

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Build
        run: go build ./...

      - name: Vet
        run: go vet ./...
      
      - name: Run Tests & Generate Reports
        run: go test -race -coverprofile=coverage.out -covermode=atomic ./... > report.json

      - name: Upload Test and Coverage Reports
        uses: actions/upload-artifact@v4
        with:
          name: test-and-coverage-reports
          path: |
            coverage.out
            report.json
          retention-days: 1

  sonarqube:
    name: SonarQube
    runs-on: ubuntu-latest
    needs: [test]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 
      - name: Download coverage report
        uses: actions/download-artifact@v4
        with:
          name: test-and-coverage-reports
          path: .
      - name: Verify coverage file
        run: |
          if [ -f coverage.out ]; then
            echo "✅ Coverage file found"
            echo "Coverage file size: $(wc -c < coverage.out) bytes"
            echo "Coverage file lines: $(wc -l < coverage.out) lines"
          else
            echo "❌ Coverage file not found!"
            exit 1
          fi
      - name: Verify report file
        run: |
          if [ -f report.json ]; then
            echo "✅ Report file found"
            echo "Report file size: $(wc -c < report.json) bytes"
            echo "Report file lines: $(wc -l < report.json) lines"
          else
            echo "❌ Report file not found!"
            exit 1
          fi
      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@5126f5d8869ff15e4a5df1598db4c5b6ff4f04c7
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: >
            -Dsonar.go.coverage.reportPaths=coverage.out
            -Dsonar.go.tests.reportPaths=report.json
            -Dsonar.sources=.
            -Dsonar.tests=.
            -Dsonar.test.inclusions=**/*_test.go
      - name: SonarQube Quality Gate check
        uses: sonarsource/sonarqube-quality-gate-action@e766c1fbc9be53c61309f7a79e6024cb51b43958
        timeout-minutes: 5
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
